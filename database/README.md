# Database

PostgreSQL schema artifacts in this directory define the authoritative data model. Apply files in lexical order through the migration workflow introduced with the backend foundation. Direct manual production changes are prohibited.

The initial schema is [0001_core.sql](schema/0001_core.sql).
