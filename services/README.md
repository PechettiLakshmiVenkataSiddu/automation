# Services

This directory is reserved for independently isolated runtime services. The executor service is introduced when browser, desktop, and constrained-code automation capabilities are implemented.
