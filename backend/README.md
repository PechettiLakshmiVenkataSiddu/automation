# Backend

The FastAPI control plane, Celery workers, scheduler composition, and domain modules will be introduced in their designated phases. The Python package configuration is maintained at the repository root to ensure a single validation policy across backend and executor code.
